# Technical Concerns

## Critical Bugs

### Top-customers report returns unsorted results
- `backend/src/modules/reports/reports.routes.ts` lines 136–140
- `.slice(0, limit)` runs before `.sort()` — customers returned in invoice-scan order, not by revenue
- Fix: add `.sort((a, b) => b.totalNet - a.totalNet)` before `.slice()`

### PDF invoices silently overflow single A4 page
- `backend/src/utils/pdf.ts` lines 92–113
- No page-break logic; items beyond ~25–30 rows clipped at page boundary

---

## Data Integrity Risks

### Invoice counter increment + insert not in transaction
- `backend/src/modules/invoices/invoices.service.ts` lines 68–84
- Two separate DB statements — concurrent load can produce duplicate invoice numbers

### Order number generated by COUNT(*)+1 — no uniqueness constraint
- `backend/src/modules/orders/orders.service.ts` lines 89–97
- Concurrent order creation can produce duplicate `WO-XXXXX` numbers; no DB constraint prevents this

### Delete-then-insert item replacement has no transaction
- `backend/src/modules/orders/orders.service.ts` lines 131–150
- `backend/src/modules/invoices/invoices.service.ts` lines 173–190
- If re-insert fails, parent record is left with zero items

---

## Security Concerns

### Google OAuth state never verified server-side
- `backend/src/modules/appointments/appointments.routes.ts` lines 30–52
- Nonce embedded in `state` but never stored or verified on callback
- Attacker can supply forged `tenantId` to connect Google Calendar to victim tenant

### Stripe webhook RLS bypass via manual `set_config` on pool connection
- `backend/src/modules/payments/payments.service.ts` lines 92–106
- If UPDATE fails, tenant GUC may not be cleared before connection returned to pool

### Email HTML includes unsanitized user data (XSS risk)
- `backend/src/modules/invoices/invoices.routes.ts` lines 98–111
- `backend/src/jobs/appointmentReminders.ts` lines 38–44
- Customer names and appointment titles interpolated directly into HTML strings

### Auth rate limit is global — no per-email brute-force protection
- `backend/src/app.ts` lines 43–47
- 100 req/min global limit; login endpoint needs per-email limiting

### Dead columns in tenants table
- `backend/src/db/schema/tenants.ts` lines 20–21
- `googleClientId` / `googleClientSecret` unused but present — misleading

---

## Performance / Scalability

### Report endpoints fetch full tables into memory
- `backend/src/modules/reports/reports.routes.ts` lines 22–53, 119–215
- No SQL aggregation; all sums computed in JavaScript over unbounded result sets

### Parts report fetches all parts to count low-stock items
- `backend/src/modules/reports/reports.routes.ts` lines 218–235
- WHERE filter should be pushed to SQL

### Per-request DB connection held for entire request lifecycle
- `backend/src/plugins/auth.ts` lines 46–69
- Pool max 20; under concurrent load causes 2-second timeouts then failures

---

## Tech Debt

### 23 `as any` casts in backend route handlers
- Heavy use in `parts.routes.ts`, `staff.routes.ts`, `vehicles.routes.ts`, `customers.routes.ts`, `orders.routes.ts`
- Bypasses Fastify typed generics entirely

### `finalLaborCost` dual-source logic in revenue-breakdown report
- `backend/src/modules/reports/reports.routes.ts` lines 192–213
- Silently switches between time-entry cost rates and invoice `unitCost`; labeled "legacy behavior" with no removal plan

### `console.error` bypasses structured logger
- `backend/src/jobs/appointmentReminders.ts` lines 52, 60

---

## Zero Test Coverage
- No test files anywhere in the project
- Financial calculations, invoice service, PDF generation — all untested
- The top-customers sort bug was only caught by manual code reading
